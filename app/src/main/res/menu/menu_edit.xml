<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menuDelete"
        android:title="@string/action_delete"
        android:icon="@drawable/ic_delete_black"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/menuAllowAddMember"
        android:checked="false"
        android:visible="false"
        android:checkable="true"
        android:title="@string/menu_allow_to_add_member"/>
    <item android:id="@+id/menuAllowPost"
        android:checked="false"
        android:visible="false"
        android:checkable="true"
        android:title="@string/menu_allow_to_add_post"/>
    <item android:id="@+id/menuAllowCommnet"
        android:checked="false"
        android:visible="false"
        android:checkable="true"
        android:title="@string/menu_allow_to_add_post_comment"/>
</menu>